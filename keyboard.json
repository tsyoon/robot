router.get('/keyboard', (req, res) => {
  const menu = {
      type: 'buttons',
      buttons: ["¸Þ´º1", "¸Þ´º2", "¸Þ´º3"]
  };

  res.set({
      'content-type': 'application/json'
  }).send(JSON.stringify(menu));
});
